# Force the deterministic local retrieval fallback for development and tests.
# Leave this unset for the real Microsoft Foundry Local runtime.
SUMMER_RAG_FORCE_FALLBACK=1

# Frontend default API URL. You can also set localStorage.ragAgentApi in the browser.
RAG_AGENT_API=http://localhost:8000
